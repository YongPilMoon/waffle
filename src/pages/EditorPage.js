import React from 'react';
import PageTemplate from 'components/common/PageTemplate';
import EmojiWrapper from 'components/editor/EditorWrapper';

const EditorPage = () => (
  <PageTemplate>
    <EmojiWrapper />
  </PageTemplate>
);

export default EditorPage;
